[tool.poetry]
name = "sped_parser"
version = "0.2.1"
description = "manipulate SPED nodes"
authors = ["Tiago Guimaraes <tilacog@protonmail.com>"]
license = "MIT"
repository = "https://gitlab.com/tilacog/sped_parser"
keywords = ["sped"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
