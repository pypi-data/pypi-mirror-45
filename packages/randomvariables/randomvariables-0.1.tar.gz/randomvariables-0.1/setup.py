from setuptools import setup

setup(name='randomvariables',
	  version='0.1',
	  description='Simple random variable generators (a NumPy wrapper)',
	  packages=['randomvariables'],
	  zip_safe=False,
	  author='Brian Wang')
