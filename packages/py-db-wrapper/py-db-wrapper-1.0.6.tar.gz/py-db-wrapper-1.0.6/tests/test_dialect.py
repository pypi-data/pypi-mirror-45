import unittest
from py_db_wrapper import dialect

class TestDialectMethods(unittest.TestCase):

    # def test_add_ticks_and_brackets(self):
    #     d = dialect.Dialect()
    #     self.assertEqual('a', d.add_ticks_and_brackets('a'))
    #     d.brackets = True
    #     d.ticks = False
    #     self.assertEqual('[a]', d.add_ticks_and_brackets('a'))
    #     d.brackets = False
    #     d.ticks = True
    #     self.assertEqual('`a`', d.add_ticks_and_brackets('a'))
    #     d.brackets = True
    #     d.ticks = True
    #     self.assertEqual('[`a`]', d.add_ticks_and_brackets('a'))
    pass
