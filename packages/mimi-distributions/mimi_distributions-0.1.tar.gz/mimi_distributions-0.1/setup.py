from setuptools import setup

setup(name='mimi_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['mimi_distributions'],
      zip_safe=False)
