# An example class to test (see tst/example/example-test.py)
# TODO delete after adding a real libpuz class with a test
class Example:

    def plus(self, a, b):
        return a + b;

    def multiply(self, a, b):
        return a * b;