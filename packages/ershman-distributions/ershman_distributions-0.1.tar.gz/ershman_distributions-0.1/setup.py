from setuptools import setup

setup(name='ershman_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['ershman_distributions'],
      zip_safe=False)
