- hosts:
    - master
    - slave
  vars_files:
    - ~/.idevops/monitor.yml
  tasks:
    - include_role:
        name: iserver
        tasks_from: "{{ task }}"
      with_items:
        - upload_config
        - restart_docker
      loop_control:
        loop_var: task
