- hosts:
    - master
    - slave
  #serial: 1
  tasks:
    - include_role:
        name: iserver
        tasks_from: "{{ task }}"
      with_items:
        - restart_docker
        - wait_docker_up
      loop_control:
        loop_var: task
